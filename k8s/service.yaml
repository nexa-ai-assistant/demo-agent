apiVersion: v1
kind: Service
metadata:
  name: retreival-agent
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: retreival-agent